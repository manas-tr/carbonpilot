apiVersion: v1
kind: Service
metadata:
  name: carbonpilot-service
spec:
  type: NodePort
  selector:
    app: carbonpilot
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30007